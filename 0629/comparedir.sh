#!/bin/bash

# input: two directories
# output: all filenames in the directory with more regular files, in sorted order

DIR1=$1
DIR2=$2

# check that they are directories
if [[ -d $DIR1 ]]; then
	if [[ -d $DIR2 ]]; then

	fi
[else 
	echo ""]
fi
