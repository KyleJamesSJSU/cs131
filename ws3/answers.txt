A1: ls -l > listing.txt
date >> listing.txt
A2: ls /nonexistent_dir 2> errors.txt
A3: ls /nonexistent_dir /etc 1> output.txt 2> errors.txt
A4: ls /nonexistent_dir /etc &> output.txt errors.txt
A5: In the terminal, I see the stdout, and no error output. /dev/null is basically the trash bin destination, so redirecting stderr to /dev/null basically got rid of it, which is why I didn't see it in the terminal. 
A6: sort fruits.txt | uniq
A7: sort fruits.txt | uniq -c | sort -nr
A8: grep ERROR server.log | wc -l
A9: grep ERROR server.log > errors_only.txt
A10: grep -oE '^[A-Z]+' server.log | sort | uniq -c | sort -r
A11: grep -oE '"state":[^,]*' /data/yelp/yelp_academic_dataset_business.json | grep -oE '"[^"]*"$' | grep -oE '[^"]*' | sort | uniq -c | sort -nr | head -5 | grep -oE '[[:alpha:]]{2}'

B1: uid=1042(ishigaki-cs9) gid=1042(ishigaki-cs9) groups=1042(ishigaki-cs9),44(video),988(docker),993(render)
uid is the User ID, and represents the ID of my current user.
gid is the Group ID, which represents the group my current user belongs to. In this case, it's the same as my UID.
B2: -rw-rw-r--
Owner: -rw (allows the owner to read and write)
Group: -rw (allows the group to read and write)
World: -r- (allows the world to read)
B3: umask returns the value 0002. According to the umask manual, this value acts as a mask for octal value 0666, which should result in 0664 for the permissions. This makes files able to be read and written by the owner and group, and only readable by the world. Testing on a new file secret.txt, it produces the default permissions -rw-rw-r-.
B4: chmod 000 secret.txt
chmod u+rw secret.txt
B5: chmod 640 fruits.txt
B6: find . -type f -perm /u+w -user $(whoami)

C1: PID = 834983
C2: nevermind we aren't doing this in class again
